<link href="https://fonts.googleapis.com/css2?family=Merriweather:ital,wght@0,300;0,400;0,700;1,300;1,400&display=swap" rel="stylesheet">
<style>
:root {
    --brand:       #C4366A;
    --brand-dark:  #9A2450;
    --brand-light: #E05488;
    --gold:        #D4903A;
    --gold-light:  #EAA84E;
    --bg:          #FFF0F5;
    --surface:     #FFFFFF;
    --surface2:    #FFF6FA;
    --border:      #F0CCDA;
    --border-2:    #FAE0EC;
    --text1:       #1E0A14;
    --text2:       #3E1828;
    --text3:       #7A4A5A;
    --text4:       #B08090;
    --success:     #2D7A4F;
    --warning:     #C47A1A;
    --info:        #2A6B9A;
    --r-sm: 10px; --r-md: 16px; --r-lg: 22px; --r-xl: 32px;
    --sh-sm: 0 1px 4px rgba(30,10,20,.07);
    --sh-md: 0 4px 20px rgba(30,10,20,.09);
    --sh-lg: 0 8px 40px rgba(30,10,20,.12);
    --sh-xl: 0 20px 60px rgba(30,10,20,.16);
}
h1,h2,h3,h4,h5 { font-family: 'Merriweather', Georgia, serif; line-height: 1.3; }
.nav-logo { font-family: 'Merriweather', serif; font-weight: 700; color: var(--brand); font-size: 1.2rem; }
.footer-brand-name { font-family: 'Merriweather', serif; }
.section-title { font-family: 'Merriweather', serif; }
.btn-primary { background: var(--brand); box-shadow: 0 2px 8px rgba(196,54,106,.28); }
.btn-primary:hover { background: var(--brand-dark); box-shadow: 0 4px 16px rgba(196,54,106,.38); }
.btn-outline { color: var(--brand); border-color: var(--brand); }
.btn-outline:hover { background: rgba(196,54,106,.06); }
.btn-gold { background: var(--gold); }
.btn-nav-register { background: var(--brand); }
.btn-nav-register:hover { background: var(--brand-dark); }
.nav-links a:hover { color: var(--brand); background: rgba(196,54,106,.06); }
.nav-links a.active { color: var(--brand); }
.btn-nav-login:hover { border-color: var(--brand); color: var(--brand); }
.section-eyebrow { color: var(--brand); }
.text-brand { color: var(--brand); }
.footer-social a:hover { background: var(--brand); border-color: var(--brand); }
.badge-brand { background: rgba(196,54,106,.1); color: var(--brand); }
.nav-logo-icon { background: var(--brand); }
.footer-brand-icon { background: var(--brand); }
</style>
