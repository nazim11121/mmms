<link href="https://fonts.googleapis.com/css2?family=DM+Serif+Display:ital@0;1&display=swap" rel="stylesheet">
<style>
:root {
    --brand:       #D4522A;
    --brand-dark:  #A83C1A;
    --brand-light: #E8733F;
    --gold:        #E8A430;
    --gold-light:  #F5BA4A;
    --bg:          #FFF6F0;
    --surface:     #FFFFFF;
    --surface2:    #FFF9F5;
    --border:      #F0D8CC;
    --border-2:    #FAE8DC;
    --text1:       #1E0F08;
    --text2:       #3E2018;
    --text3:       #7A4A38;
    --text4:       #B08070;
    --success:     #2D7A4F;
    --warning:     #C47A1A;
    --info:        #2A6B9A;
    --r-sm: 9px; --r-md: 16px; --r-lg: 22px; --r-xl: 32px;
    --sh-sm: 0 1px 4px rgba(30,15,8,.07);
    --sh-md: 0 4px 20px rgba(30,15,8,.09);
    --sh-lg: 0 8px 40px rgba(30,15,8,.12);
    --sh-xl: 0 20px 60px rgba(30,15,8,.16);
}
h1,h2,h3,h4,h5 { font-family: 'DM Serif Display', Georgia, serif; }
.nav-logo { font-family: 'DM Serif Display', serif; font-size: 1.4rem; color: var(--brand); letter-spacing: -.01em; }
.footer-brand-name { font-family: 'DM Serif Display', serif; }
.section-title { font-family: 'DM Serif Display', serif; }
.btn-primary { background: var(--brand); box-shadow: 0 2px 8px rgba(212,82,42,.28); }
.btn-primary:hover { background: var(--brand-dark); box-shadow: 0 4px 16px rgba(212,82,42,.38); }
.btn-outline { color: var(--brand); border-color: var(--brand); }
.btn-outline:hover { background: rgba(212,82,42,.06); }
.btn-gold { background: var(--gold); }
.btn-nav-register { background: var(--brand); }
.btn-nav-register:hover { background: var(--brand-dark); }
.nav-links a:hover { color: var(--brand); background: rgba(212,82,42,.06); }
.nav-links a.active { color: var(--brand); }
.btn-nav-login:hover { border-color: var(--brand); color: var(--brand); }
.section-eyebrow { color: var(--brand); }
.text-brand { color: var(--brand); }
.footer-social a:hover { background: var(--brand); border-color: var(--brand); }
.badge-brand { background: rgba(212,82,42,.1); color: var(--brand); }
.nav-logo-icon { background: var(--brand); }
.footer-brand-icon { background: var(--brand); }
</style>
