<link href="https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap" rel="stylesheet">
<style>
:root {
    --brand:       #1E6B47;
    --brand-dark:  #165238;
    --brand-light: #28885C;
    --gold:        #B5891A;
    --gold-light:  #D4A820;
    --bg:          #F0F7F2;
    --surface:     #FFFFFF;
    --surface2:    #F6FAF7;
    --border:      #C8DDD2;
    --border-2:    #DAEAE0;
    --text1:       #0F2B1A;
    --text2:       #1E4A2C;
    --text3:       #4A7A5A;
    --text4:       #8AB09A;
    --success:     #1E6B47;
    --warning:     #D97706;
    --info:        #1E6B8C;
    --r-sm: 6px; --r-md: 12px; --r-lg: 18px; --r-xl: 24px;
    --sh-sm: 0 1px 4px rgba(15,43,26,.06);
    --sh-md: 0 4px 20px rgba(15,43,26,.09);
    --sh-lg: 0 8px 40px rgba(15,43,26,.12);
    --sh-xl: 0 20px 60px rgba(15,43,26,.16);
}
h1,h2,h3,h4,h5 { font-family: 'Lora', Georgia, serif; }
.nav-logo { font-family: 'Lora', serif; color: var(--brand); }
.footer-brand-name { font-family: 'Lora', serif; }
.section-title { font-family: 'Lora', serif; }
.btn-primary { background: var(--brand); box-shadow: 0 2px 8px rgba(30,107,71,.28); }
.btn-primary:hover { background: var(--brand-dark); box-shadow: 0 4px 16px rgba(30,107,71,.38); }
.btn-outline { color: var(--brand); border-color: var(--brand); }
.btn-outline:hover { background: rgba(30,107,71,.06); }
.btn-nav-register { background: var(--brand); }
.btn-nav-register:hover { background: var(--brand-dark); }
.nav-links a:hover { color: var(--brand); background: rgba(30,107,71,.06); }
.nav-links a.active { color: var(--brand); }
.btn-nav-login:hover { border-color: var(--brand); color: var(--brand); }
.section-eyebrow { color: var(--brand); }
.text-brand { color: var(--brand); }
.footer-social a:hover { background: var(--brand); border-color: var(--brand); }
.badge-brand { background: rgba(30,107,71,.1); color: var(--brand); }
.nav-logo-icon { background: var(--brand); }
.footer-brand-icon { background: var(--brand); }
.footer-contact-item i { color: var(--gold); }
</style>
