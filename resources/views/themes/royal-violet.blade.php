<link href="https://fonts.googleapis.com/css2?family=EB+Garamond:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap" rel="stylesheet">
<style>
:root {
    --brand:       #6B35A8;
    --brand-dark:  #4E2080;
    --brand-light: #8B55C8;
    --gold:        #C88B3A;
    --gold-light:  #E8A84E;
    --bg:          #F7F2FF;
    --surface:     #FFFFFF;
    --surface2:    #FBF8FF;
    --border:      #D8CBF0;
    --border-2:    #E8E0F8;
    --text1:       #1A0F2E;
    --text2:       #3A2558;
    --text3:       #6A4A8A;
    --text4:       #9A7AB8;
    --success:     #2D7A4F;
    --warning:     #C47A1A;
    --info:        #3B5BDB;
    --r-sm: 8px; --r-md: 14px; --r-lg: 20px; --r-xl: 28px;
    --sh-sm: 0 1px 4px rgba(26,15,46,.07);
    --sh-md: 0 4px 20px rgba(26,15,46,.10);
    --sh-lg: 0 8px 40px rgba(26,15,46,.14);
    --sh-xl: 0 20px 60px rgba(26,15,46,.18);
}
h1,h2,h3,h4,h5 { font-family: 'EB Garamond', Georgia, serif; }
.nav-logo { font-family: 'EB Garamond', serif; font-size: 1.45rem; color: var(--brand); }
.footer-brand-name { font-family: 'EB Garamond', serif; }
.section-title { font-family: 'EB Garamond', serif; }
.btn-primary { background: var(--brand); box-shadow: 0 2px 8px rgba(107,53,168,.28); }
.btn-primary:hover { background: var(--brand-dark); box-shadow: 0 4px 16px rgba(107,53,168,.38); }
.btn-outline { color: var(--brand); border-color: var(--brand); }
.btn-outline:hover { background: rgba(107,53,168,.06); }
.btn-nav-register { background: var(--brand); }
.btn-nav-register:hover { background: var(--brand-dark); }
.nav-links a:hover { color: var(--brand); background: rgba(107,53,168,.06); }
.nav-links a.active { color: var(--brand); }
.btn-nav-login:hover { border-color: var(--brand); color: var(--brand); }
.section-eyebrow { color: var(--brand); }
.text-brand { color: var(--brand); }
.footer-social a:hover { background: var(--brand); border-color: var(--brand); }
.badge-brand { background: rgba(107,53,168,.1); color: var(--brand); }
.nav-logo-icon { background: var(--brand); }
.footer-brand-icon { background: var(--brand); }
</style>
